[project]
name = "ansible-role-haproxy"
version = "1.0.0"
description = "Development dependencies for the HAProxy Ansible role"
requires-python = ">=3.14"

dependencies = [
    "ansible>=13.3.0",
    "ansible-lint>=26.1.1",
    "molecule>=25.12.0",
    "molecule-docker>=2.1.0",
    "pytest-testinfra>=10.2.2",
    "yamllint>=1.38.0",
]
